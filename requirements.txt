# Core HTTP & image
requests>=2.31.0
Pillow>=10.3.0
numpy>=1.26.0

# Subject isolation (Style B – "Subject Sandwich")
# Uses ONNX runtime under the hood; first run downloads the model (~170 MB)
rembg>=2.0.57
onnxruntime>=1.17.0

# OpenRouter (free models - no extra package needed, uses requests)

# Environment variables
python-dotenv>=1.0.1
